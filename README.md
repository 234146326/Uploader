# Uploader
Uploader pack
    // +----------------------------------------------------------------------
    // 设置图片上传
    // +----------------------------------------------------------------------
    "IMGupload" =>[
        "savePath" => "upload/" ,             //存储文件夹
        "maxSize" => 1000 ,                   //允许的文件最大尺寸，单位KB
        "allowFiles" => [".gif" , ".png" , ".jpg" , ".jpeg" , ".bmp" ]  //允许的文件格式
    ],